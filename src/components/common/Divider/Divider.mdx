import { ArgTypes, Canvas, Meta } from "@storybook/addon-docs";
import * as DividerStories from "./Divider.stories";

<Meta of={DividerStories} />

# Divider

## Overview

Divider는 시각적으로나 의미적으로 콘텐츠를 구분하는 컴포넌트입니다.

## Props

### direction props

- `Divider 방향`을 정의하는 props로, `direction` props로 지정할 수 있습니다.

<Canvas of={DividerStories.DirectionProps} />

## API

<ArgTypes of={DividerStories} />
