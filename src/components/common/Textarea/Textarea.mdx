import { ArgTypes, Canvas, Meta } from "@storybook/addon-docs";
import * as TextareaStories from "./Textarea.stories";

<Meta of={TextareaStories} />

# Textarea

## Overview

Textarea는 다수의 텍스트 입력이 가능한 컴포넌트입니다.

<Canvas of={TextareaStories.Primary} />
