import { ArgTypes, Canvas, Meta } from "@storybook/addon-docs";
import * as AvatarStories from "./Avatar.stories";

<Meta of={AvatarStories} />

# Avatar

## Overview

Avatar는 프로필 이미지를 나타내는 컴포넌트입니다.

## Props

### outline props

- `Avatar 경계선`을 정의하는 props로, `outline` props로 지정할 수 있습니다.

<Canvas of={AvatarStories.OutlineProps} />

### size props

- `Avatar 크기`를 정의하는 props로, `size` props로 지정할 수 있습니다.

<Canvas of={AvatarStories.SizeProps} />

### src props

- `Avatar 내부 이미지`를 정의하는 props로, `src` props로 지정할 수 있습니다.

<Canvas of={AvatarStories.SrcProps} />

## API

<ArgTypes of={AvatarStories} />
